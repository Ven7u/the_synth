[package]
name = "synth-engine"
version = "0.1.0"
edition = "2021"

[dependencies]
synth-dsp    = { path = "../synth-dsp" }
synth-common = { path = "../synth-common" }
fundsp       = { version = "0.23", features = ["files", "fft"] }
